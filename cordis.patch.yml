# dsh-git-worktree bundle patch: one insert over the profile root, adding the
# plugin row. Later profile layers and the user's own cordis.patch.yml address
# this row by id; the last write wins per row.

- insert:
    - id: dsh-git-worktree
      name: dsh-git-worktree
